<UserControl x:Class="SaintSender.UI.Views.ReadMailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:SaintSender.UI.Views"
             xmlns:utils="clr-namespace:SaintSender.UI.Utils"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid d:DataContext="{d:DesignInstance Type=local:MainWindow}">
        <Grid.RowDefinitions>
            <RowDefinition Height="50*"/>
            <RowDefinition Height="50*"/>
            <RowDefinition Height="356*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="130*"/>
            <ColumnDefinition Width="250*"/>
        </Grid.ColumnDefinitions>
        <TextBlock Text="{Binding Path=SelectedMail.Subject}" Grid.ColumnSpan="2"/>
        <TextBlock Text="{Binding Path=SelectedMail.Date}" Grid.Row="1"/>
        <TextBlock Text="{Binding Path=SelectedMail.Sender}" Grid.Row="1" Grid.Column="1"/>
        <WebBrowser Name="browserMessageDisplay" utils:BrowserBehavior.Html="{Binding Path=SelectedMail.Message}" Grid.Row="2" Grid.ColumnSpan="2"/>
    </Grid>
</UserControl>
